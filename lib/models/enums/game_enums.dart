enum AttackingStyle { balanced, possession, direct, counter }

enum DefensiveStyle { lowBlock, midBlock, highPress }

enum PressingIntensity { low, medium, high }

enum MatchEventType { goal, assist, yellowCard, redCard, injury, substitution }

enum TransferType { buy, sell, loan }

enum TransferStatus { listed, negotiating, completed, rejected }

enum TrainingType { attacking, defensive, physical, tactical }

enum FacilityType { stadium, trainingGround, medicalCenter, youthFacilities }

enum BoardObjectiveType {
  leaguePosition,
  avoidRelegation,
  cupRun,
  increaseRevenue,
  developYouth,
}

enum AchievementType {
  firstWin,
  leagueChampion,
  domesticDouble,
  undefeatedSeason,
  youthStarDeveloped,
  promotion,
  unbeatenHome,
  topScorer,
}

enum AuthMode { email, google, guest }
